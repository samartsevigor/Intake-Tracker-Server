const {
  DATABASE_USER,
  DATABASE_NAME,
  DATABASE_PASSWORD,
  DATABASE_PORT,
  SECRET_KEY,
  SERVER_PORT,
} = process.env;

module.exports = {
  DATABASE_USER,
  DATABASE_NAME,
  DATABASE_PASSWORD,
  DATABASE_PORT,
  SECRET_KEY,
  SERVER_PORT,
};
